73-                 /* add in the command sequence */
74-                 getecnam(ktp->k_code, &outseq[cpos], SIZEOF(outseq) - cpos);
75- 
76-                 /* and add it as a line into the buffer */
77:-                if ( addline(listbuf, outseq) != TRUE )
78-+                if ( ! addline(listbuf, outseq) ) {
79-                     return (FALSE);
80-+                }
81- 
--
84-@@ -472,9 +473,10 @@ int PASCAL NEAR buildlist P2_(int, type, CONST char *, mstring)
85-         /* if no key was bound, we need to dump it anyway */
86-         if ( cpos > 0 ) {
87-             outseq[cpos] = 0;
88:-            if ( addline(listbuf, outseq) != TRUE )
89-+            if ( ! addline(listbuf, outseq) ) {
90-                 return (FALSE);
91-             }
92-+        }
--
133-                 /* add in the command sequence */
134-                 getecnam(ktp->k_code, &outseq[cpos], SIZEOF(outseq) - cpos);
135- 
136-                 /* and add it as a line into the buffer */
137:-                if ( addline(listbuf, outseq) != TRUE )
138-+                if ( ! addline(listbuf, outseq) ) {
139-                     return (FALSE);
140-+                }
141- 
--
144-@@ -520,9 +527,10 @@ fail:   /* and on to the next name */
145-         }
146- 
147-         /* add a blank line between the key and macro lists */
148:-        if ( first_entry == TRUE ) {
149:-            if ( addline(listbuf, "") != TRUE )
150-+        if ( first_entry )  {
151-+            if ( ! addline(listbuf, "") ) {
152-                 return (FALSE);
153-+            }
--
157-@@ -530,9 +538,10 @@ fail:   /* and on to the next name */
158-         /* if no key was bound, we need to dump it anyway */
159-         if ( cpos > 0 ) {
160-             outseq[cpos] = 0;
161:-            if ( addline(listbuf, outseq) != TRUE )
162-+            if ( ! addline(listbuf, outseq) ) {
163-                 return (FALSE);
164-             }
165-+        }
