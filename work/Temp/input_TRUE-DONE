797-+    maxlen  = MIN2(SIZEOF(buf) - 1, MAX2(maxlen, 0));
798-+
799-     /* if we are executing a command line get the next arg and match it */
800-     if ( clexec ) {
801:-        if ( macarg(buf) != TRUE )
802--            return (NULL);
803:+        if ( macarg(buf, SIZEOF(buf)) != TRUE ) return (NULL);
804- 
805-         return (buf);
806-     }
807-@@ -1119,13 +1553,18 @@ static char *PASCAL NEAR  complete P4_(CONST char *,  prompt,
